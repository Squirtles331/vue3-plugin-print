# Changelog

本项目遵循语义化版本。重大不兼容变更、功能、修复和安全更新会记录在此。

## [0.2.0] - Unreleased

### Added

- 独立 Vue 3 浏览器打印模板设计器，包含十类元素、属性编辑、JSON 数据绑定和运行时预览/打印。
- `TemplateDocument v1`、本地模板库、导入导出、起始模板和元素预设。
- 本地模板删除与清空操作、GitHub Pages 构建支持、CI、浏览器打印隔离回归测试和发布验收文档。
- 严格打印预检、运行时数据路径绑定、问题定位、模板纸张 CSS、资源就绪等待和多页表格的受控元素重复。
- 打包消费者入口验证与发布标签—包版本一致性校验，避免错误标签发布错误 tarball。

### Changed

- 打印默认会阻止缺失绑定、越过模板安全区、空条码/二维码和不可加载图片。需要保留旧模板不完整输出的调用方可显式设置 `printPolicy: { allowIncomplete: true }`，但该选项不应作为长期替代修复的方案。
- 纸张尺寸以模板毫米值写入打印 CSS；模板边距现在作为安全区进行预检。实际打印机的物理不可打印边距仍须在设备验收中校准。
- 多页表格仅重复显式标记 `repeatPerPage` 的元素，且不再为已无数据的表格生成空后续片段。

### Security

- 升级 `nanoid` 至 3.3.18、`postcss` 至 8.5.26，修复审计发现的间接依赖问题。

### Deferred

- PDF/图片导出、静默/云打印、设备级打印控制、服务端渲染、协作、npm 组件包和 Web Component。
