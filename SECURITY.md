# 安全策略

## 支持范围

首发版本为 `v0.1.x`。安全修复会优先覆盖当前 `main` 分支和最新发布标签。

## 报告漏洞

请不要在公开 Issue 中披露可利用的漏洞、凭据或真实业务数据。请将复现步骤、影响范围、受影响版本和可行修复建议发送至 `2644648810@qq.com`，主题注明 `Print Template Studio security`。

维护者会在 7 个自然日内确认收到报告，并在评估后沟通修复和公开披露时间。公开披露前请勿将漏洞细节扩散到 Issue、讨论区或第三方平台。

## 安全边界

- 模板导入会校验版本与结构；脚本型字段不会执行。
- 运行时 JSON 只能提供内容和集合数据，不能注入样式、布局或任意代码。
- v0.1 不提供账户、服务端存储或云打印服务。浏览器本地模板的设备安全、浏览器配置和业务数据脱敏由部署者负责。
